from itertools import permutations

class Solution:
    def totalNumbers(self, digits: List[int]) -> int:
        s = set()

        for val in permutations(digits, 3):
            if val[0] != 0 and val[2] % 2 == 0:
                s.add(val)

        return len(s)
