class Solution(object):
    def smallestIndex(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        for i in range(0,len(nums)):
            result = sum(int(digit) for digit in str(nums[i]))
            if(result == i):
                return i

        return -1
